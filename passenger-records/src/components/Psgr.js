import React from "react";

const Psgr = ({ firstname, lastname, email, passport, frqtflyer }) => {
  firstname = `${firstname}`;
  lastname = `${lastname}`;
  email = `${email}`;
  passport = `${passport}`;
  frqtflyer = `${frqtflyer}`;
};

export default Psgr;
